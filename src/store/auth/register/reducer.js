
import {  } from './actionType';

const initialState = {
   
};

const register = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            state = { ...state };
            break;
    }
    return state;
};

export default register;